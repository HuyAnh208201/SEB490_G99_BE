spring.application.name=api
server.port=4313

spring.datasource.url=jdbc:mysql://72.61.114.184:3306/chuoi_cua_hang
spring.datasource.username=chuoi_cua_hang
spring.datasource.password=PGDTf3rsW5TcanxW

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# false = fast boot against shared remote DB (schema/catalog already applied).
# true  = run all *Migration / *Seeder beans once (empty DB or intentional re-seed).
app.startup.bootstrap-enabled=false

# DB chuoi_cua_hang đã có schema + FK — không tự động ALTER bảng (tránh lỗi FK)
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.jdbc.batch_size=50
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true
spring.jpa.properties.hibernate.default_batch_fetch_size=50

spring.security.user.name=admin
spring.security.user.password=Mk123!@#
spring.security.user.roles=ADMIN

# Hidden / externalized — set via environment variables (defaults are for local dev)
url.client-url=${CLIENT_URL:http://localhost:5175}
url.api-url=${API_URL:http://localhost:4313}

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.protocol=smtp

# Hidden / externalized ? set MAIL_USERNAME and MAIL_PASSWORD via environment variables
spring.mail.username=${MAIL_USERNAME:anhlhhe153263@fpt.edu.vn}
spring.mail.password=${MAIL_PASSWORD:rnacfqjoxqcfndxc}


spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.ssl.enable=false

spring.mail.default-encoding=UTF-8
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000
spring.mail.properties.mail.transport.protocol=smtp
spring.mail.properties.mail.debug=true

# Demo OTP for email verification (branch suspend, critical user actions, email verify).
# Default true for local/demo; set OTP_MOCK=false in production.
otp.mock=${OTP_MOCK:true}
otp.mock-code=${OTP_MOCK_CODE:123456}

# TODO: replace with per-product reorder_point from DB when available.
purchase-request.default-reorder-point=10

# Auto-deactivate ACTIVE promotions after endAt (ms between runs).
campaign.expiry-check-ms=60000

# Quỹ tiền mặt mặc định cho ca đầu tiên của ngày (VNĐ). Các ca sau kế thừa
# actual_cash của ca liền trước khi ca đó đóng.
shift.default-opening-cash=2000000

# Team QA: allow cashiers to open/close shifts outside branch operating hours.
shift.test-mode.allow-outside-hours=true

# Auto-close cashier sessions still open this many minutes after shift end time.
shift.auto-close.grace-minutes=30
# How often the overdue-shift job runs (ms).
shift.overdue-check-ms=300000

# Tỉ lệ tích/đổi điểm. Server là nguồn sự thật, FE chỉ đọc để hiển thị.
loyalty.vnd-per-point=10000
loyalty.point-value-vnd=1000

# ── PayOS – cổng thanh toán QR chuyển khoản ──────────────────────────
payos.client-id=${PAYOS_CLIENT_ID:d6d42b23-c7a7-486e-bbe6-26d19277f24f}
payos.api-key=${PAYOS_API_KEY:752521e6-7474-41a8-bf6c-db88ea316c9e}
payos.checksum-key=${PAYOS_CHECKSUM_KEY:65287b5717f511a42212513f21c5a34861e988a5fce6037a49205490ce7ed87a}
payos.return-url=${PAYOS_RETURN_URL:http://localhost:5173/payment/success}
payos.cancel-url=${PAYOS_CANCEL_URL:http://localhost:5173/payment/cancel}